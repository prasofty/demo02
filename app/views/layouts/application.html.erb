<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/html">
<head>
  <title>Demo02</title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Demo02", root_path, :class => "brand" %>
      <div class="nav-collapse collapse pull-right">
        <ul class="nav">
          <li class="active">
            <%= link_to "Articles", articles_path %>
          </li>
          <li>
            <%= link_to "New Article", new_article_path %>
          </li>
          <% if current_user %>
            <li>
              <%= link_to "Hi #{current_user_profile.name}", "#" %>
            </li>
            <li class="dropdown">
              <%= link_to "#", :class=>"dropdown-toggle", "data-toggle"=>"dropdown" do %>
                <i class="icon-cog icon-white"></i>
                <ul class="dropdown-menu header-dropdown-menu">
                  <li>
                    <%= link_to 'My Profile', profile_path(current_user_profile) %>
                  </li>
                  <li>
                    <%= link_to 'My Articles', profiles_path(:anchor => 'my_jobs') %>
                  </li>
                </ul>
              <% end %>
            </li>
            <li>
              <%= link_to "Sign Out", destroy_user_session_path, :method => :delete %>
            </li>
          <% else %>
            <li>
              <%= link_to "Sign In", new_user_session_path %>
            </li>
            <li>
              <%= link_to "Sign Up", new_user_registration_path %>
            </li>
          <% end %>

        </ul>
      </div>
      <!--/.nav-collapse -->
    </div>
  </div>
</div>

<div class="container">
  <% flash.each do |name, msg| %>
    <%= content_tag :div, :class => "alert #{name == :error ? "alert-error" : "alert-success" }" do %>
      <a class="close" data-dismiss="alert" href="#">×</a>
      <%= msg %>
    <% end %>
  <% end %>
  <div class="content">
    <%= yield %>
  </div>
</div>

</body>
</html>
